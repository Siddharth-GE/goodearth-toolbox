// Dexter private helpers shared across its action files, queries and the
// public viewer route — never copy-pasted between them.

export const GRANT = "/dexter";

/** The public viewer's path prefix — the one prefix in lib/supabase/proxy.ts. */
export const DECK_PATH_PREFIX = "/deck/";
