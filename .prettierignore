# Generated by `npm run db:types` — reformatting it would produce a huge
# diff every time the schema changes, and nobody reads it by hand.
lib/supabase/database.types.ts

# Migrations are SQL and are applied by hand in Supabase Studio; Prettier
# has opinions about SQL that would rewrite files already run against the
# production database, making the file no longer match what was applied.
supabase/migrations/

.next/
node_modules/
data/
